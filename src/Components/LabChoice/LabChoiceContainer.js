import LabChoice from "./LabChoice";
import {connect} from 'react-redux'
let mapStateToProps = (state) =>
{
    return{

    }
}

let LabChoiceContainer = connect(mapStateToProps) (LabChoice)
export default LabChoiceContainer